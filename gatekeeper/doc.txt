https://github.com/open-policy-agent/frameworks/tree/master/constraint

Constraints spec.match spec.parameters field. 
Input.review field from rego file
3 types of audit : prom metrics, logs of audit pod(configure it), constraint’s status
Constraints spec.enforcementAction can have value dryRun, warn, deny
Syncing data to the cache is possible via Config. Cached data are accessible in rego file under data.inventory . ex : data.inventory.namespace[<namespace>][groupVersion][<kind>][<name>]

Exempting ns using spec.match[].excludeNamespace field in Config. Or by flag
Customizing startup & admission behavior
To read: Metrics, Debugging, recovery, cloud, failing


Assign
	applyTo
	Match
	Location string
	AssignParameters

AssignMetadata
	Match
	Location string
	AssignMetadataParameters

ModifySet
	applyTo
	Match
	Location string
	ModifysetParameters

AssignParameters
	pathTests []pathtest
	Assign assignField
AssignMetadataParameters
	pathTests []pathtest
AssignMetadataParameters
	pathTests []pathtest
	Operation -> merge/prune
Values struct {FromList []interface{} `json:"fromList,omitempty"`}
Match & applyTo  are described here

